Module-scoped agent instructions live alongside the module. See
`bigtable/AGENTS.md` for the bigtable Session subsystem.

This repository is a monorepo: firestore, spanner, storage, and other
modules may add their own `AGENTS.md` under their module directory as
they need module-specific guidance.
